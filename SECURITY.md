# 安全说明

## 报告漏洞

请**不要**在公开 issue 中提交涉及真实数据或利用细节的安全问题。请通过 GitHub 的 **Security Advisories（私有报告）** 功能提交，或私信联系仓库维护者，我们会尽快响应。

## 设计中的安全基线（原型期已实现）

- 密码 bcrypt 哈希存储；JWT（HS256，有过期时间）；密钥只进 `.env`，仓库只提供 `.env.example`。
- 前端输入不可信：金额、状态、归属由服务端重新验证；SQL 全部参数化。
- 订单金额由服务端按数据库价格重算；限库存原子扣减防超卖；幂等键防重复下单。
- 顾客查单必须「手机号 + 订单号」同时匹配；商家订单列表手机号脱敏，详情仅授权门店可见。
- 所有商家管理接口校验门店归属，越权返回 403；操作留审计日志。
- CSV 导出对用户可控字段做公式注入转义。
- 种子管理员为默认密码时，首次登录强制修改密码（`must_change_password`）。

## 已知边界（当前未覆盖，待阶段四/五）

- 无 HTTPS 部署；本地 http 接口仅供开发/内网原型使用，不得直接暴露公网。
- 微信登录签名校验、支付回调验签与退款、第三方核销接口签名均未实现（原型期不接入）。
- CORS 当前默认宽松（本地调试），部署前必须按域名收紧。
- 顾客手机号属个人信息：仅用于订单联系与查询；上线前需按微信平台隐私政策补充协议文案并评估加密/脱敏方案。

## 报告什么内容

- 越权访问他人门店/订单数据
- 金额篡改、重复下单、超卖
- 敏感信息泄露（密钥、数据库文件、用户隐私）
- 注入类问题（SQL/CSV/路径）
- 默认凭据相关风险
